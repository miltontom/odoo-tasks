<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="attendance_shd_action" model="ir.cron">
        <field name="name">Generate absentees list</field>
        <field name="model_id" ref="model_attendance"/>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
<!--        <field name="nextcall"-->
<!--               eval="(DateTime.now() + timedelta(minutes=1))"/>-->
        <field name="state">code</field>
        <field name="code">model.generate_absentees()</field>
    </record>
</odoo>