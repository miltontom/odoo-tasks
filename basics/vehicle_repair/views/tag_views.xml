<odoo>
    <record id="vehicle_repair_tag_action" model="ir.actions.act_window">
        <field name="name">Tags</field>
        <field name="res_model">tag</field>
        <field name="view_mode">list,form</field>
    </record>

    <record id="vehicle_repair_tag_list" model="ir.ui.view">
        <field name="name">tag.list</field>
        <field name="model">tag</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="color" widget="color_picker"/>
            </list>
        </field>
    </record>

    <record id="vehicle_repair_tag_form" model="ir.ui.view">
        <field name="name">tag.form</field>
        <field name="model">tag</field>
        <field name="arch" type="xml">
            <form>
                <field name="name"/>
                <field name="color" widget="color_picker"/>
            </form>
        </field>
    </record>

</odoo>