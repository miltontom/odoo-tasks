Add a field `quantity`, `from_unit`, `to_unit`, `converted_quantity`, find the converted quantity by converting unit _x_ to unit _y_. `to_unit` should only allow units under the same unit category.

NOTE: I'm using the sale order form just for the sake of doing it.
